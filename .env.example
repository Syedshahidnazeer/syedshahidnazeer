# Canonical site URL — used for metadataBase, canonical link, OG image and sitemap.
# Optional on Vercel (falls back to the production URL). Set it once a custom domain is live.
NEXT_PUBLIC_SITE_URL=https://syedshahidnazeer.vercel.app
